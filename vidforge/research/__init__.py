"""Topic research: what already exists on YouTube for a topic, and what an LLM makes of it.

    youtube.py  — search + statistics (Data API v3 with YOUTUBE_API_KEY, else the public search
                  page's ytInitialData through plain HTTP; no login needed for either)
    analyze.py  — prompt for the browser-chat bridge: worth doing? angles? titles? hooks?
"""
