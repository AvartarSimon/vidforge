import { Box, Typography } from '@mui/material'

// Scaffold placeholder — replaced by the real shell (NavRail + step content) in a later commit.
export default function App() {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h5">vidforge (React) — 脚手架就绪</Typography>
    </Box>
  )
}
